"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const dom_matrix_1 = require("./dom-matrix");
class CSSPerspective {
    constructor(length) {
        this.length = length;
        this.is2D = false;
        if (!length.type.has('length')) {
            throw new TypeError(`Failed to construct CSSSkew: ax and ay must be angles`);
        }
    }
    toMatrix() {
        const matrix = new dom_matrix_1.DOMMatrix();
        matrix.m34 = -1 / this.length.to('px').value;
        return matrix;
    }
    toString() {
        return `perspective(${this.length})`;
    }
}
exports.CSSPerspective = CSSPerspective;
//# sourceMappingURL=css-perspective.js.map