import { CSSTransformComponent } from './css-transform-value';
import { CSSNumericValue } from './css-numeric-value';
import { DOMMatrix } from './dom-matrix';
export declare class CSSTranslate implements CSSTransformComponent {
    x: CSSNumericValue;
    y: CSSNumericValue;
    z?: CSSNumericValue;
    readonly is2D: boolean;
    toMatrix(): DOMMatrix;
    constructor(x: CSSNumericValue, y: CSSNumericValue, z?: CSSNumericValue);
    toString(): string;
}
