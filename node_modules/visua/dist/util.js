"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const logger_1 = require("./logger");
exports.removeLeadingDashes = (property) => {
    return property.replace(/^-+/, '');
};
exports.removeQuotes = (string) => {
    return string.replace(/(?:^['"]|['"]$)/g, '');
};
exports.toCamelCase = (property) => {
    return property.replace(/-([a-zA-Z0-9_])/g, (match, letter) => letter.toUpperCase());
};
exports.inRange = (num, min, max) => num >= min && num <= max;
exports.restrict = (num, min, max) => {
    return Math.min(max, Math.max(min, num));
};
exports.map = (num, inMin, inMax, outMin, outMax) => {
    return (num - inMin) * (outMax - outMin) / (inMax - inMin) + outMin;
};
exports.mod = (num, mod) => {
    return ((num % mod) + mod) % mod;
};
exports.toRad = (angle) => angle * Math.PI / 180;
exports.warnAt = (message, location) => {
    logger_1.logger.warn(`${message}\n    at ${location.source}:${location.start.line}:${location.start.column}`);
};
//# sourceMappingURL=util.js.map