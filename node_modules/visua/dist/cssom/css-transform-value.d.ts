import { DOMMatrix } from './dom-matrix';
export interface CSSTransformComponent {
    is2D: boolean;
    toMatrix(): DOMMatrix;
}
export declare class CSSTransformValue {
    private transforms;
    readonly length: number;
    constructor(transforms: CSSTransformComponent[]);
    get(index: number): CSSTransformComponent;
    append(component: CSSTransformComponent): number;
    readonly is2D: boolean;
    toMatrix(): DOMMatrix;
    toString(): string;
}
